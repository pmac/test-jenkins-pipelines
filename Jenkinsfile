switch ( env.BRANCH_NAME ) {
    case "master":
        echo "Building master"
        // build '../other-repo/master'
    case /demo__.+/:
        echo "Building a demo: ${env.BRANCH_NAME}"
    default:
        echo "Doing nothing for ${enb.BRANCH_NAME}"
}
